- [ ] T1 — Create `src/hello_harness.sh` with `usage()` and flag parsing.
      Covers: R1, R2, R3, R4.
- [ ] T2 — Implement default greeting output when no `--name` flag.
      Covers: R1, R4.
- [ ] T3 — Implement `--name <name>` overrides the default greeting.
      Covers: R2.
- [ ] T4 — Implement `--help` prints usage and exits 0.
      Covers: R3.
- [ ] T5 — Create `tests/test_hello_harness.sh` covering R1–R4.
      Covers: R1, R2, R3, R4.
